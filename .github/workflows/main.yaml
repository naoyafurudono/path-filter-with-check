name: main

on:
  workflow_dispatch:
  push:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "hello world"
  call-sub1:
    needs: main
    uses: ./.github/workflows/sub1.yaml
